# คู่มือการตั้งค่า Google Sheets สำหรับการบันทึกข้อมูล (Write Access)

ปัญหาที่พบคือ **"Public Spreadsheet cannot be written to"** เกิดจากระบบความปลอดภัยของ Google API ที่ไม่อนุญาตให้แอปพลิเคชันภายนอกเขียนข้อมูลลงใน Sheet ที่แชร์แบบสาธารณะโดยไม่มีการยืนยันตัวตน (Authentication)

เพื่อให้แอปฯ สามารถบันทึกข้อมูลได้ คุณต้องใช้ **Service Account** ตามขั้นตอนดังนี้ครับ:

---

## 🏗️ ขั้นตอนที่ 1: สร้าง Service Account ใน Google Cloud
1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
2. สร้างโปรเจกต์ใหม่ (หรือใช้โปรเจกต์เดิมที่มีอยู่)
3. ไปที่เมนู **APIs & Services > Library**
4. ค้นหาและกด **Enable** 2 ตัวนี้:
   - `Google Sheets API`
   - `Google Drive API`
5. ไปที่เมนู **APIs & Services > Credentials**
6. กด **+ CREATE CREDENTIALS** และเลือก **Service Account**
7. ตั้งชื่อ Service Account (เช่น `streamlit-app`) แล้วกด **Create and Continue**
8. ในส่วน Role (Optional) สามารถเลือกเป็น `Editor` หรือปล่อยว่างไว้ก็ได้ แล้วกด **Done**

## 🔑 ขั้นตอนที่ 2: สร้าง JSON Key
1. ในหน้า Credentials คุณจะเห็นรายชื่อ Service Account ที่เพิ่งสร้าง ให้คลิกเข้าไป
2. ไปที่แท็บ **Keys**
3. กด **ADD KEY > Create new key**
4. เลือกรูปแบบเป็น **JSON** แล้วกด **Create**
5. ไฟล์จะถูกดาวน์โหลดลงเครื่องคอมพิวเตอร์ของคุณ **(ห้ามแชร์ไฟล์นี้ให้คนอื่น!)**

## 📤 ขั้นตอนที่ 3: แชร์ไฟล์ Google Sheet
1. เปิดไฟล์ Google Sheet ของคุณ
2. กดปุ่ม **Share** (แชร์) ที่มุมขวาบน
3. **สำคัญมาก:** คัดลอก **Email ของ Service Account** (ที่ลงท้ายด้วย `.iam.gserviceaccount.com`) จากไฟล์ JSON หรือหน้า Console
4. วาง Email นั้นในช่องเชิญ และตั้งค่าสิทธิ์เป็น **Editor**
5. กดปุ่ม **Send** (แชร์ให้เรียบร้อย)

## 🛠️ ขั้นตอนที่ 4: ตั้งค่าในแอปฯ (.streamlit/secrets.toml)
ในโปรเจกต์ของคุณ ให้เปิดไฟล์ `.streamlit/secrets.toml` และนำข้อมูลจากไฟล์ JSON มาใส่ดังนี้:

```toml
[connections.gsheets]
spreadsheet = "https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit"

# ข้อมูลจากไฟล์ JSON ที่ดาวน์โหลดมา
type = "service_account"
project_id = "..."
private_key_id = "..."
private_key = "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
client_email = "..."
client_id = "..."
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certificates"
client_x509_cert_url = "..."
```

---

---

### � ขั้นตอนที่ 5: การนำขึ้น Streamlit Cloud (share.streamlit.io)
เมื่อคุณอัปโหลดโค้ดลง GitHub แล้ว และต้องการเปิดแอปบน Streamlit Cloud ให้ทำตามนี้ครับ:

1. **อย่าเอาไฟล์ `secrets.toml` หรือ JSON ขึ้น GitHub**: นี่เป็นเรื่องสำคัญมากเพื่อความปลอดภัย ให้สร้างไฟล์ `.gitignore` และใส่ชื่อไฟล์ความลับเหล่านี้ลงไป
2. ไปที่หน้า Dashboard ของ [Streamlit Cloud](https://share.streamlit.io/)
3. กดที่แอปของคุณ และเลือก **Settings**
4. ไปที่เมนู **Secrets**
5. คัดลอกเนื้อหาทั้งหมดจากไฟล์ `.streamlit/secrets.toml` ในเครื่องของคุณ ไปวางลงในช่องข้อความบนเว็บ
6. กด **Save**

### ⚠️ ข้อควรระวัง (Security Warning)
- **GitHub Public**: หาก Repository ของคุณเป็นสาธารณะ (Public) และคุณเผลออัปโหลดไฟล์ `secrets.toml` ขึ้นไป คนอื่นจะสามารถเข้าถึง Google Drive และข้อมูลของคุณได้ทันที
- **.gitignore**: ควรมีไฟล์ชื่อ `.gitignore` อยู่ในโฟลเดอร์หลัก และมีข้อความข้างในดังนี้:
  ```text
  .streamlit/secrets.toml
  *.json
  assessment_results.csv
  __pycache__/
  ```

ทำแบบนี้แอปจะสามารถเชื่อมต่อ Google Sheets บน Cloud ได้อย่างปลอดภัยครับ!
